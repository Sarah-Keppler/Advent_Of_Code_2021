#include <iostream>
#include <fstream>
#include <vector>
#include <string>

int main()
{
    std::ifstream file{"input.txt"};
    std::string line{};
    unsigned int y{0}, x{0}, aim{0};
    while (std::getline(file, line))
    {
    }
}